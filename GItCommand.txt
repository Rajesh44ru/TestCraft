GIt From CLone
  git clone url
  git status
  git add <FileName>
  git commit -m "Message"
  git push origin main

GIt CLone From Local
  git init
   git remote add orgin <link>
  git remote -v  (To verify branch)
  git branch  (To check Branch)
  git branch -m main  (To rename )
  git push origin main   ( or git push -u oring main)

  GLobal
git config --global user.name "Rajesh44ru"
git config --global user.email "rsingh.rajesh01@gmail.com"
git config-- list

#
GIT Branch
 git branch ( to Check Branch)
 git branch -m main (to rename Branch)
 git checkout <branchname> (to navigate branch)
 git checkout -b <new branch> ( to create new branch)
git branch -d <branchname> ( to delete branch)